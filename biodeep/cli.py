"""Console entry point for the BioDeep CLI."""

from __future__ import annotations

import sys


def main() -> int:
    # Minimal CLI stub until core runtime is wired.
    print("BioDeep CLI (deepagents) is installed. Add runtime wiring here.")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
